<%= render 'layouts/errors', :object => @image %>

<%= form_for [@user, @image] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <%= f.file_field :avatar %>
  <p><%= f.submit %></p>
<% end %>
